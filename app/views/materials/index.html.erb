<p id="notice"><%= notice %></p>

<h1>Materials</h1>

<!--
<table>
  <thead>
    <tr>
      <th>Content type</th>
      <th>Content value</th>
      <th>Status</th>
      <th>Internal comment</th>
      <th>External comment</th>
      <th>Created user</th>
      <th>Updated user</th>
      <th>Active</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @materials.each do |material| %>
      <tr>
        <td><%= material.content_type %></td>
        <td><%= material.content_value %></td>
        <td><%= material.status %></td>
        <td><%= material.internal_comment %></td>
        <td><%= material.external_comment %></td>
        <td><%= material.created_user_id %></td>
        <td><%= material.updated_user_id %></td>
        <td><%= material.active %></td>
        <td><%= link_to 'Show', material %></td>
        <td><%= link_to 'Edit', edit_material_path(material) %></td>
        <td><%= link_to 'Destroy', material, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
-->

<div class="row">
<% @materials.each do |material| %>
  <div class="col col-sm-6 col-xs-12 ">
    <div class="panel panel-default">
      <div class="panel-body text-center">
        <%= image_tag material.content_value, :size => '255x255', :style => 'object-fit: cover' %>
      </div>
      <div class="panel-footer">
        <p> 
          <%= material.external_comment %>
        </p> 
      </div>
    </div>
  </div>
<% end %>
</div>

<br>

<%= link_to 'New Material', new_material_path %>
